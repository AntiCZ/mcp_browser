# BrowserMCP Enhanced - Default Environment

# HTTP MCP Server
BROWSER_MCP_HTTP_PORT=3000

# WebSocket Daemon
BROWSER_MCP_DAEMON_PORT=8765
BROWSER_MCP_HTTP_URL=http://127.0.0.1:${BROWSER_MCP_HTTP_PORT}

# Command timeout (ms)
BROWSER_MCP_COMMAND_TIMEOUT=45000

# Enable HTTP server debug endpoint (/debug/session/<id>)
BROWSER_MCP_ENABLE_DEBUG=0

# Optional: Supabase REST for storage ingestion (local defaults)
# SUPABASE_REST_URL=http://127.0.0.1:54321
# SUPABASE_SERVICE_KEY=sb_secret_your_local_service_key
